{
  "author": "Rakib Mahmud Mridha",
  "name": "collabspace-web-app",
  "license": "ISC",
  "version": "1.0.0",
  "scripts": {
    "dev": "bun --filter '*' dev",
    "build": "bun --filter '*' build",
    "start": "bun --filter '*' start",
    "dev:client": "bun --filter ./client dev",
    "dev:server": "bun --filter ./server dev",
    "lint": "bun --filter '*' lint",
    "lint:fix": "bun --filter '*' lint:fix",
    "typecheck": "bun --fiter '*' typecheck"
  },
  "workspaces": [
    "server",
    "packages/*"
  ],
  "dependencies": {
    "@t3-oss/env-core": "^0.11.1",
    "client": "^0.0.1",
    "eslint-plugin-tailwindcss": "^3.17.5",
    "zod": "^3.24.1"
  }
}
